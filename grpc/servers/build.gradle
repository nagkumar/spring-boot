subprojects {
    defaultTasks 'clean', 'runDep', 'bootRun'

    dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-actuator'
	implementation 'com.teja.sb.grpc:spec-pb:1.1'
    }

    task runDep {
	dependsOn(":spec:pb:publishToMavenLocal")
    }
}